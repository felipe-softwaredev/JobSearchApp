model User {
    username     String              @id @unique
    password     String
    first_name   String
    last_name    String
    email        String              @unique
    applications ApplicatonsOnJobs[]
}
